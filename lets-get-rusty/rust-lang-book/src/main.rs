// Drop trait, allows you to customise what happens when a variable goes out of scope
// The drop trait is almost always implemented when we use smart pointers
// In the MyBox trait, when it drops because it went out of the scope,
// we want to deallocate the data stored on the heap
fn main() {
     
}